# Default to the host target. The Linux musl build used by release artifacts
# is selected explicitly via `cargo build --target x86_64-unknown-linux-musl`.

[target.x86_64-unknown-linux-musl]
linker = "rust-lld"
